<div class="offcanvas-body" id="navbarResponsive">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {% for item in Model.Items %}
        {{ item | shape_render }}
        {% endfor %}
    </ul>
</div>
